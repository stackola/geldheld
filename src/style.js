import colors from "./colors";

let space = 12;

let style = {
  space: space,
  bigBorderRadius: 8,
  smallBorderRadius: 4,
  containerPadding: 8,
  text: {
    color: colors.text,
    fontSize: 14
  },
  containerHeadline: {
    color: colors.text,
    fontSize: 16,
    fontWeight: "bold",
    marginTop: space / 2,
    marginBottom: space / 2
  }
};

export default style;
